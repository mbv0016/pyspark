Defining Schema for dataFrame ( manually )
Defining Haeder for data frame ( manually)
Add two columns to new data frame
outputs ( column level or dataframe or anythng else)
df_pyspark.na.drop().show() ( SS of the output )
df_pyspark.na.drop(how='any', subset=['Age']).show() ( SS of the output)
current timestamp & get date from current timestamp